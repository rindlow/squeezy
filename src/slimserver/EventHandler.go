package slimserver

import (
	//"fmt"
	//"log"
)


// The EvenHandler should be started with a master-chan to the StreamServer
// and the client-Server. On these master channels the instance-channels
// should be passed. These instance channels are then uses for passing
// events and messages.

// Keep a state-machine instance for each known device 
